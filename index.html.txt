<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>FleetBook</title>
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;500;600;700;800&family=Nunito:wght@400;500;600;700&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.31/jspdf.plugin.autotable.min.js"></script>
<style>
  :root {
    --orange:#FF6B00; --orange-light:#FF8C00; --dark:#1A1A2E; --card:#16213E;
    --card2:#0F3460; --text:#E0E0E0; --text-muted:#8892A4;
    --green:#00C896; --red:#FF4757; --blue:#4A90D9;
    --border:rgba(255,255,255,0.08); --radius:16px;
  }
  *{margin:0;padding:0;box-sizing:border-box;}
  body{font-family:'Nunito',sans-serif;background:var(--dark);color:var(--text);min-height:100vh;overflow-x:hidden;}
  h1,h2,h3{font-family:'Baloo 2',sans-serif;}
  #auth-screen{min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:24px;background:linear-gradient(135deg,#1A1A2E 0%,#16213E 50%,#0F3460 100%);}
  .auth-logo{font-family:'Baloo 2',sans-serif;font-size:2.5rem;font-weight:800;color:var(--orange);margin-bottom:4px;}
  .auth-tagline{color:var(--text-muted);font-size:0.9rem;margin-bottom:36px;}
  .auth-box{background:var(--card);border-radius:24px;padding:32px 28px;width:100%;max-width:400px;border:1px solid var(--border);box-shadow:0 24px 64px rgba(0,0,0,0.4);}
  .auth-tabs{display:flex;gap:8px;margin-bottom:24px;}
  .auth-tab{flex:1;padding:10px;border:none;border-radius:10px;cursor:pointer;font-family:'Nunito',sans-serif;font-size:0.95rem;font-weight:600;background:rgba(255,255,255,0.05);color:var(--text-muted);transition:all 0.2s;}
  .auth-tab.active{background:var(--orange);color:white;}
  .form-group{margin-bottom:16px;}
  .form-group label{display:block;font-size:0.85rem;color:var(--text-muted);margin-bottom:6px;font-weight:600;}
  .form-group input{width:100%;padding:12px 16px;border-radius:12px;border:1px solid var(--border);background:rgba(255,255,255,0.05);color:var(--text);font-family:'Nunito',sans-serif;font-size:1rem;outline:none;transition:border 0.2s;}
  .form-group input:focus{border-color:var(--orange);}
  .btn-primary{width:100%;padding:14px;border:none;border-radius:12px;background:var(--orange);color:white;font-family:'Baloo 2',sans-serif;font-size:1.1rem;font-weight:700;cursor:pointer;transition:all 0.2s;margin-top:8px;}
  .auth-error{color:var(--red);background:rgba(255,71,87,0.1);padding:10px;border-radius:8px;font-size:0.85rem;margin-top:12px;text-align:center;}
  #main-app{display:none;min-height:100vh;padding-bottom:80px;}
  .topbar{background:var(--card);border-bottom:1px solid var(--border);padding:14px 20px;display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:100;}
  .topbar-logo{font-family:'Baloo 2',sans-serif;font-weight:800;font-size:1.4rem;color:var(--orange);}
  .signout-btn{background:rgba(255,71,87,0.15);color:var(--red);border:none;border-radius:8px;padding:6px 12px;font-size:0.8rem;cursor:pointer;font-family:'Nunito',sans-serif;font-weight:600;}
  .sync-dot{width:8px;height:8px;border-radius:50%;background:var(--green);display:inline-block;margin-right:6px;animation:pulse 2s infinite;}
  @keyframes pulse{0%,100%{opacity:1;}50%{opacity:0.4;}}
  .bottom-nav{position:fixed;bottom:0;left:0;right:0;background:var(--card);border-top:1px solid var(--border);display:flex;z-index:200;}
  .nav-item{flex:1;display:flex;flex-direction:column;align-items:center;padding:10px 4px;cursor:pointer;transition:all 0.2s;border:none;background:none;color:var(--text-muted);}
  .nav-item.active{color:var(--orange);}
  .nav-icon{font-size:1.4rem;margin-bottom:2px;}
  .nav-label{font-size:0.65rem;font-weight:600;}
  .page{display:none;padding:20px;}
  .page.active{display:block;}
  .page-header{margin-bottom:20px;}
  .page-header h2{font-family:'Baloo 2',sans-serif;font-size:1.5rem;font-weight:800;}
  .page-header p{color:var(--text-muted);font-size:0.85rem;margin-top:2px;}
  .card{background:var(--card);border-radius:var(--radius);padding:20px;margin-bottom:16px;border:1px solid var(--border);}
  .card-title{font-family:'Baloo 2',sans-serif;font-size:1rem;font-weight:700;color:var(--text);margin-bottom:16px;}
  .summary-row{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:16px;}
  .summary-pill{background:var(--card);border-radius:14px;padding:16px;border:1px solid var(--border);text-align:center;}
  .summary-pill .label{font-size:0.75rem;color:var(--text-muted);font-weight:600;margin-bottom:4px;}
  .summary-pill .value{font-family:'Baloo 2',sans-serif;font-size:1.4rem;font-weight:800;}
  .value.green{color:var(--green);}.value.red{color:var(--red);}.value.orange{color:var(--orange);}.value.blue{color:var(--blue);}
  .input-group{margin-bottom:14px;}
  .input-group label{display:block;font-size:0.82rem;color:var(--text-muted);margin-bottom:5px;font-weight:600;}
  .input-group input,.input-group select{width:100%;padding:11px 14px;border-radius:10px;border:1px solid var(--border);background:rgba(255,255,255,0.05);color:var(--text);font-family:'Nunito',sans-serif;font-size:0.95rem;outline:none;transition:border 0.2s;}
  .input-group input:focus,.input-group select:focus{border-color:var(--orange);}
  .input-group select option{background:#1A1A2E;}
  .btn{padding:11px 20px;border:none;border-radius:10px;cursor:pointer;font-family:'Nunito',sans-serif;font-weight:700;font-size:0.95rem;transition:all 0.2s;}
  .btn-orange{background:var(--orange);color:white;}
  .btn-blue{background:var(--blue);color:white;}
  .btn-whatsapp{background:#25D366;color:white;}
  .btn-red{background:rgba(255,71,87,0.15);color:var(--red);border:1px solid rgba(255,71,87,0.3);}
  .btn-ghost{background:rgba(255,255,255,0.06);color:var(--text);border:1px solid var(--border);}
  .btn-full{width:100%;}
  .two-col{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
  .tabs{display:flex;gap:8px;margin-bottom:20px;overflow-x:auto;padding-bottom:4px;}
  .tab-btn{flex-shrink:0;padding:8px 18px;border:1px solid var(--border);border-radius:20px;background:transparent;color:var(--text-muted);cursor:pointer;font-family:'Nunito',sans-serif;font-weight:600;font-size:0.85rem;transition:all 0.2s;white-space:nowrap;}
  .tab-btn.active{background:var(--orange);color:white;border-color:var(--orange);}
  .trip-row{display:grid;grid-template-columns:90px 80px 1fr 70px 30px;gap:6px;align-items:center;margin-bottom:8px;padding:8px;background:rgba(255,255,255,0.04);border-radius:10px;border:1px solid var(--border);}
  .trip-row input{padding:7px 8px;border-radius:8px;border:1px solid var(--border);background:rgba(255,255,255,0.05);color:var(--text);font-family:'Nunito',sans-serif;font-size:0.82rem;outline:none;width:100%;}
  .del-trip-btn{background:rgba(255,71,87,0.15);color:var(--red);border:none;border-radius:6px;width:28px;height:28px;cursor:pointer;font-size:0.9rem;}
  .client-item{display:flex;align-items:center;justify-content:space-between;padding:14px 16px;background:rgba(255,255,255,0.04);border-radius:12px;margin-bottom:8px;border:1px solid var(--border);}
  .client-avatar{width:42px;height:42px;border-radius:50%;background:var(--orange);display:flex;align-items:center;justify-content:center;font-family:'Baloo 2',sans-serif;font-weight:700;font-size:1.1rem;color:white;flex-shrink:0;margin-right:12px;}
  .client-info{flex:1;}
  .client-name{font-weight:700;font-size:0.95rem;}
  .client-rate{font-size:0.8rem;color:var(--green);font-weight:600;}
  .client-detail{font-size:0.75rem;color:var(--text-muted);}
  .icon-btn{width:32px;height:32px;border-radius:8px;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:1rem;}
  .icon-btn-red{background:rgba(255,71,87,0.15);}
  .icon-btn-blue{background:rgba(74,144,217,0.15);}
  .bill-card{background:linear-gradient(135deg,var(--card) 0%,var(--card2) 100%);border-radius:16px;padding:16px;margin-bottom:12px;border:1px solid var(--border);}
  .bill-header{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:8px;}
  .bill-client{font-weight:700;font-size:0.95rem;}
  .bill-date{font-size:0.78rem;color:var(--text-muted);}
  .bill-amount{font-family:'Baloo 2',sans-serif;font-size:1.5rem;font-weight:800;color:var(--orange);}
  .bill-detail{font-size:0.78rem;color:var(--text-muted);margin-top:2px;}
  .bill-actions{display:flex;gap:6px;margin-top:10px;flex-wrap:wrap;}
  .modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.7);z-index:500;display:flex;align-items:flex-end;justify-content:center;opacity:0;pointer-events:none;transition:opacity 0.3s;}
  .modal-overlay.open{opacity:1;pointer-events:all;}
  .modal{background:var(--card);border-radius:24px 24px 0 0;padding:28px 24px;width:100%;max-width:500px;max-height:90vh;overflow-y:auto;transform:translateY(100%);transition:transform 0.3s;}
  .modal-overlay.open .modal{transform:translateY(0);}
  .modal-title{font-family:'Baloo 2',sans-serif;font-size:1.2rem;font-weight:800;margin-bottom:20px;}
  .modal-close{float:right;background:rgba(255,255,255,0.08);border:none;border-radius:8px;width:32px;height:32px;cursor:pointer;color:var(--text);font-size:1rem;}
  .company-profile-box{background:linear-gradient(135deg,rgba(255,107,0,0.12),rgba(255,107,0,0.04));border:1px solid rgba(255,107,0,0.3);border-radius:14px;padding:14px 16px;margin-bottom:16px;}
  .company-profile-name{font-family:'Baloo 2',sans-serif;font-size:1.1rem;font-weight:800;color:var(--orange);}
  .company-profile-detail{font-size:0.8rem;color:var(--text-muted);margin-top:2px;}
  .invoice-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px;}
  .invoice-actions .btn{padding:10px 8px;font-size:0.82rem;text-align:center;}
  .toast{position:fixed;bottom:100px;left:50%;transform:translateX(-50%) translateY(20px);background:var(--card2);color:white;padding:12px 24px;border-radius:20px;font-weight:600;font-size:0.9rem;z-index:999;opacity:0;transition:all 0.3s;white-space:nowrap;border:1px solid var(--border);box-shadow:0 8px 24px rgba(0,0,0,0.4);}
  .toast.show{opacity:1;transform:translateX(-50%) translateY(0);}
  .toast.success{border-color:var(--green);}.toast.error{border-color:var(--red);}
  .loading{text-align:center;padding:40px;color:var(--text-muted);}
  .spinner{width:32px;height:32px;border:3px solid var(--border);border-top-color:var(--orange);border-radius:50%;animation:spin 0.8s linear infinite;margin:0 auto 12px;}
  @keyframes spin{to{transform:rotate(360deg);}}
  .empty-state{text-align:center;padding:48px 24px;color:var(--text-muted);}
  .empty-icon{font-size:3rem;margin-bottom:12px;}
  .empty-state h3{color:var(--text);margin-bottom:8px;font-family:'Baloo 2',sans-serif;}
  .report-row{display:flex;justify-content:space-between;align-items:center;padding:12px 0;border-bottom:1px solid var(--border);}
  .report-row:last-child{border-bottom:none;}
  .gst-badge{display:inline-block;padding:2px 8px;border-radius:20px;font-size:0.7rem;font-weight:700;background:rgba(74,144,217,0.15);color:var(--blue);margin-left:6px;}
</style>
</head>
<body>
<div id="auth-screen">
  <div class="auth-logo">🚛 FleetBook</div>
  <div class="auth-tagline">Transport ka hisaab — bilkul aasaan</div>
  <div class="auth-box">
    <div class="auth-tabs">
      <button class="auth-tab active" onclick="window.showAuthTab('signin')">Sign In</button>
      <button class="auth-tab" onclick="window.showAuthTab('signup')">Sign Up</button>
    </div>
    <div id="signin-form">
      <div class="form-group"><label>Email</label><input type="email" id="signin-email" placeholder="aapka@email.com"></div>
      <div class="form-group"><label>Password</label><input type="password" id="signin-password" placeholder="••••••••"></div>
      <button class="btn-primary" onclick="window.doSignIn()">Sign In →</button>
      <div id="signin-error" class="auth-error" style="display:none;"></div>
    </div>
    <div id="signup-form" style="display:none;">
      <div class="form-group"><label>Full Name</label><input type="text" id="signup-name" placeholder="Chandan Singh"></div>
      <div class="form-group"><label>Email</label><input type="email" id="signup-email" placeholder="aapka@email.com"></div>
      <div class="form-group"><label>Password</label><input type="password" id="signup-password" placeholder="Min 6 characters"></div>
      <button class="btn-primary" onclick="window.doSignUp()">Create Account →</button>
      <div id="signup-error" class="auth-error" style="display:none;"></div>
    </div>
  </div>
</div>

<div id="main-app">
  <div class="topbar">
    <div class="topbar-logo">🚛 FleetBook</div>
    <div style="display:flex;align-items:center;gap:10px;">
      <span style="font-size:0.75rem;color:var(--text-muted);"><span class="sync-dot"></span>Live</span>
      <button class="signout-btn" onclick="window.doSignOut()">Sign Out</button>
    </div>
  </div>

  <div class="page active" id="page-home">
    <div class="page-header"><h2 id="greeting">Namaste! 👋</h2><p>Overview</p></div>
    <div class="summary-row">
      <div class="summary-pill"><div class="label">Is Mahine</div><div class="value orange" id="home-month-billing">₹0</div></div>
      <div class="summary-pill"><div class="label">Total Bills</div><div class="value green" id="home-total-bills">0</div></div>
      <div class="summary-pill"><div class="label">Clients</div><div class="value blue" id="home-clients">0</div></div>
      <div class="summary-pill"><div class="label">Mahine Expense</div><div class="value red" id="home-month-expense">₹0</div></div>
    </div>
    <div class="card">
      <div class="card-title">📋 Recent Bills</div>
      <div id="home-recent-bills"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>
  </div>

  <div class="page" id="page-create">
    <div class="page-header"><h2>📝 Bill Banao</h2><p>KRISHNA LOGISTICS format</p></div>
    <div class="card">
      <div class="card-title">👤 Client Details</div>
      <div class="input-group">
        <label>Client *</label>
        <select id="bill-client" onchange="window.onBillClientChange()">
          <option value="">-- Client chuniye --</option>
        </select>
      </div>
      <div id="bill-client-preview" style="display:none;margin-bottom:14px;">
        <div style="background:rgba(255,107,0,0.08);border:1px solid rgba(255,107,0,0.25);border-radius:10px;padding:12px;font-size:0.82rem;">
          <div id="bcp-name" style="font-weight:700;color:var(--orange);font-size:0.95rem;"></div>
          <div id="bcp-gstin" style="color:var(--blue);"></div>
          <div id="bcp-addr" style="color:var(--text-muted);"></div>
        </div>
      </div>
      <div class="two-col">
        <div class="input-group"><label>Bill Date *</label><input type="date" id="bill-date"></div>
        <div class="input-group"><label>Bill No. *</label><input type="text" id="bill-no" placeholder="5"></div>
      </div>
      <div class="two-col">
        <div class="input-group">
          <label>GST Type</label>
          <select id="bill-gst-type" onchange="window.recalcTotal()">
            <option value="none">No GST</option>
            <option value="5">GST 5%</option>
            <option value="12">GST 12%</option>
            <option value="18">GST 18%</option>
          </select>
        </div>
        <div class="input-group"><label>FY</label><input type="text" id="bill-fy" placeholder="25-26"></div>
      </div>
    </div>

```
<div class="card">
  <div class="card-title">🚛 Trips</div>
  <div style="display:grid;grid-template-columns:90px 80px 1fr 70px 30px;gap:6px;margin-bottom:6px;padding:0 2px;">
    <div style="font-size:0.7rem;color:var(--text-muted);font-weight:700;">DATE</div>
    <div style="font-size:0.7rem;color:var(--text-muted);font-weight:700;">VEHICLE</div>
    <div style="font-size:0.7rem;color:var(--text-muted);font-weight:700;">DESTINATION</div>
    <div style="font-size:0.7rem;color:var(--text-muted);font-weight:700;">RATE</div>
    <div></div>
  </div>
  <div id="trips-container"></div>
  <button class="btn btn-ghost btn-full" style="margin-top:8px;" onclick="window.addTripRow()">+ Trip Add Karo</button>
  <div style="margin-top:16px;padding-top:16px;border-top:1px solid var(--border);">
    <div style="display:flex;justify-content:space-between;margin-bottom:6px;">
      <span style="color:var(--text-muted);font-size:0.85rem;">Subtotal</span>
      <span style="font-family:'Baloo 2',sans-serif;font-size:1.1rem;font-weight:800;" id="trip-subtotal">₹0</span>
    </div>
    <div id="gst-breakdown" style="display:none;">
      <div style="display:flex;justify-content:space-between;font-size:0.82rem;color:var(--text-muted);margin-bottom:4px;">
        <span id="cgst-label">CGST</span><span id="cgst-val">₹0</span>
      </div>
      <div style="display:flex;justify-content:space-between;font-size:0.82rem;color:var(--text-muted);margin-bottom:4px;">
        <span id="sgst-label">SGST</span><span id="sgst-val">₹0</span>
      </div>
    </div>
    <div style="display:flex;justify-content:space-between;padding-top:10px;border-top:1px solid var(--border);">
      <span style="font-weight:700;">TOTAL</span>
      <span style="font-family:'Baloo 2',sans-serif;font-size:1.5rem;font-weight:800;color:var(--green);" id="trip-total">₹0</span>
    </div>
  </div>
</div>
<div class="two-col" style="margin-bottom:20px;">
  <button class="btn btn-orange" onclick="window.saveBill(false)">💾 Save</button>
  <button class="btn btn-blue" onclick="window.saveBill(true)">🧾 Save + Invoice</button>
</div>
```

  </div>

  <div class="page" id="page-billing">
    <div class="page-header"><h2>💰 Bills</h2><p>Saari bills</p></div>
    <div class="tabs">
      <button class="tab-btn active" onclick="window.switchBillingTab('all')">Sab</button>
      <button class="tab-btn" onclick="window.switchBillingTab('month')">Is Mahine</button>
      <button class="tab-btn" onclick="window.switchBillingTab('gst')">GST</button>
    </div>
    <div class="card" style="padding:14px;">
      <div class="two-col">
        <div class="input-group" style="margin:0;"><label>Client</label><select id="billing-filter-client" onchange="window.loadBills()"><option value="">Sab</option></select></div>
        <div class="input-group" style="margin:0;"><label>Mahina</label><input type="month" id="billing-filter-month" onchange="window.loadBills()"></div>
      </div>
    </div>
    <div id="billing-summary" class="summary-row"></div>
    <div id="bills-list"><div class="loading"><div class="spinner"></div>Loading...</div></div>
  </div>

  <div class="page" id="page-clients">
    <div class="page-header"><h2>👥 Clients</h2></div>
    <button class="btn btn-orange btn-full" style="margin-bottom:16px;" onclick="window.openModal('add-client-modal')">+ Naya Client</button>
    <div id="clients-list"><div class="loading"><div class="spinner"></div>Loading...</div></div>
  </div>

  <div class="page" id="page-expense">
    <div class="page-header"><h2>🚛 Expenses</h2></div>
    <div class="tabs">
      <button class="tab-btn active" onclick="window.switchExpenseTab('add')">Add</button>
      <button class="tab-btn" onclick="window.switchExpenseTab('list')">Records</button>
    </div>
    <div id="expense-tab-add">
      <div class="card">
        <div class="input-group"><label>Date</label><input type="date" id="exp-date"></div>
        <div class="two-col">
          <div class="input-group"><label>Category</label><select id="exp-category"><option value="Fuel">⛽ Fuel</option><option value="Driver Salary">👨‍✈️ Salary</option><option value="Maintenance">🔧 Maintenance</option><option value="Toll">🛣️ Toll</option><option value="Other">📦 Other</option></select></div>
          <div class="input-group"><label>Vehicle</label><input type="text" id="exp-vehicle" placeholder="Truck no."></div>
        </div>
        <div class="two-col">
          <div class="input-group"><label>Amount (₹)</label><input type="number" id="exp-amount" placeholder="0"></div>
          <div class="input-group"><label>Payment</label><select id="exp-payment"><option value="Cash">💵 Cash</option><option value="Transfer">🏦 Transfer</option></select></div>
        </div>
        <div class="input-group"><label>Note</label><input type="text" id="exp-note" placeholder="Details..."></div>
        <button class="btn btn-orange btn-full" onclick="window.saveExpense()">💾 Save</button>
      </div>
    </div>
    <div id="expense-tab-list" style="display:none;">
      <div class="card" style="padding:14px;"><div class="input-group" style="margin:0;"><label>Mahina</label><input type="month" id="exp-filter-month" onchange="window.loadExpenses()"></div></div>
      <div id="expenses-list"></div>
    </div>
  </div>

  <div class="page" id="page-settings">
    <div class="page-header"><h2>⚙️ Settings</h2></div>
    <div class="company-profile-box">
      <div class="company-profile-name" id="preview-company-name">Company Set Nahi</div>
      <div class="company-profile-detail" id="preview-company-gstin" style="color:var(--blue);font-weight:700;"></div>
      <div class="company-profile-detail" id="preview-company-address"></div>
      <div class="company-profile-detail" id="preview-company-phone"></div>
      <div class="company-profile-detail" id="preview-company-bank"></div>
    </div>
    <div class="card">
      <div class="card-title">🏢 Company Profile</div>
      <div class="input-group"><label>Company Naam *</label><input type="text" id="co-name" placeholder="KRISHNA LOGISTICS"></div>
      <div class="input-group"><label>Address</label><input type="text" id="co-address" placeholder="14/1/1A, Jackson Lane KOL-700001"></div>
      <div class="input-group"><label>GSTIN</label><input type="text" id="co-gstin" placeholder="19DSEPS6197D1Z6" style="text-transform:uppercase;"></div>
      <div class="input-group"><label>Mobile</label><input type="tel" id="co-phone" placeholder="9007774838"></div>
      <div class="input-group"><label>Bank Name</label><input type="text" id="co-bank-name" placeholder="STATE BANK OF INDIA, CHOWRINGHEE"></div>
      <div class="input-group"><label>Account No.</label><input type="text" id="co-acc" placeholder="44375171733"></div>
      <div class="input-group"><label>IFSC</label><input type="text" id="co-ifsc" placeholder="SBIN0001054"></div>
      <button class="btn btn-orange btn-full" onclick="window.saveCompanyProfile()">💾 Save</button>
    </div>
    <div class="card" style="background:rgba(255,71,87,0.05);border-color:rgba(255,71,87,0.2);">
      <button class="btn btn-red btn-full" onclick="window.doSignOut()">🚪 Sign Out</button>
    </div>
  </div>

  <div class="bottom-nav">
    <button class="nav-item active" onclick="window.showPage('home')"><div class="nav-icon">🏠</div><div class="nav-label">Home</div></button>
    <button class="nav-item" onclick="window.showPage('create')"><div class="nav-icon">➕</div><div class="nav-label">New Bill</div></button>
    <button class="nav-item" onclick="window.showPage('billing')"><div class="nav-icon">💰</div><div class="nav-label">Bills</div></button>
    <button class="nav-item" onclick="window.showPage('clients')"><div class="nav-icon">👥</div><div class="nav-label">Clients</div></button>
    <button class="nav-item" onclick="window.showPage('settings')"><div class="nav-icon">⚙️</div><div class="nav-label">Settings</div></button>
  </div>
</div>

<div class="modal-overlay" id="add-client-modal">
  <div class="modal">
    <button class="modal-close" onclick="window.closeModal('add-client-modal')">✕</button>
    <div class="modal-title">👤 Naya Client</div>
    <div class="input-group"><label>Naam *</label><input type="text" id="client-name" placeholder="PRASEED EXIM LLP"></div>
    <div class="input-group"><label>Billing Address</label><input type="text" id="client-billing-address" placeholder="4A, Karaballa Mohammed Street"></div>
    <div class="input-group"><label>GSTIN</label><input type="text" id="client-gstin" placeholder="19AAUFP6417C1ZX" style="text-transform:uppercase;"></div>
    <div class="input-group"><label>Phone</label><input type="tel" id="client-phone" placeholder="9876543210"></div>
    <div class="input-group"><label>Default Rate/Trip (₹)</label><input type="number" id="client-rate" placeholder="1200"></div>
    <button class="btn btn-orange btn-full" onclick="window.saveClient()">✅ Save</button>
  </div>
</div>

<div class="modal-overlay" id="edit-client-modal">
  <div class="modal">
    <button class="modal-close" onclick="window.closeModal('edit-client-modal')">✕</button>
    <div class="modal-title">✏️ Edit Client</div>
    <input type="hidden" id="edit-client-id">
    <div class="input-group"><label>Naam *</label><input type="text" id="edit-client-name"></div>
    <div class="input-group"><label>Billing Address</label><input type="text" id="edit-client-billing-address"></div>
    <div class="input-group"><label>GSTIN</label><input type="text" id="edit-client-gstin" style="text-transform:uppercase;"></div>
    <div class="input-group"><label>Phone</label><input type="tel" id="edit-client-phone"></div>
    <div class="input-group"><label>Default Rate (₹)</label><input type="number" id="edit-client-rate"></div>
    <button class="btn btn-orange btn-full" onclick="window.updateClient()">✅ Update</button>
  </div>
</div>

<div class="modal-overlay" id="invoice-modal">
  <div class="modal">
    <button class="modal-close" onclick="window.closeModal('invoice-modal')">✕</button>
    <div class="modal-title">🧾 Invoice</div>
    <div id="invoice-preview-box"></div>
    <div class="invoice-actions">
      <button class="btn btn-blue" onclick="window.downloadInvoicePDF()">📥 PDF</button>
      <button class="btn btn-ghost" onclick="window.printInvoice()">🖨️ Print</button>
      <button class="btn btn-whatsapp" onclick="window.shareInvoiceWA()">💬 WhatsApp</button>
      <button class="btn btn-ghost" onclick="window.copyInvoiceText()">📋 Copy</button>
    </div>
  </div>
</div>

<div class="toast" id="toast"></div>

<script type="module">
import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
import { getAuth, createUserWithEmailAndPassword, signInWithEmailAndPassword, signOut, onAuthStateChanged, updateProfile } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-auth.js";
import { getFirestore, collection, addDoc, getDocs, doc, updateDoc, deleteDoc, setDoc, getDoc, query, where, orderBy, Timestamp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";

const firebaseConfig = {
  apiKey: "AIzaSyAy62c8duu7_xOVolKtgT4kHWGmRq6TlzU",
  authDomain: "fleet-book-bdf9f.firebaseapp.com",
  projectId: "fleet-book-bdf9f",
  storageBucket: "fleet-book-bdf9f.firebasestorage.app",
  messagingSenderId: "411659069887",
  appId: "1:411659069887:web:7d1ee21e5d3c172ba9990a"
};

const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);

let currentUser=null,clientsCache=[],currentBillingTab='all',currentExpenseTab='add',currentInvoiceBill=null,companyProfile={};

window.showAuthTab=showAuthTab; window.doSignIn=doSignIn; window.doSignUp=doSignUp; window.doSignOut=doSignOut;
window.showPage=showPage; window.openModal=openModal; window.closeModal=closeModal;
window.saveClient=saveClient; window.updateClient=updateClient; window.deleteClient=deleteClient; window.editClient=editClient;
window.onBillClientChange=onBillClientChange; window.addTripRow=addTripRow; window.removeTripRow=removeTripRow; window.recalcTotal=recalcTotal;
window.saveBill=saveBill; window.deleteBill=deleteBill; window.viewInvoice=viewInvoice;
window.downloadInvoicePDF=downloadInvoicePDF; window.printInvoice=printInvoice; window.shareInvoiceWA=shareInvoiceWA; window.copyInvoiceText=copyInvoiceText;
window.saveExpense=saveExpense; window.deleteExpense=deleteExpense; window.switchBillingTab=switchBillingTab; window.switchExpenseTab=switchExpenseTab;
window.loadBills=loadBills; window.loadExpenses=loadExpenses; window.saveCompanyProfile=saveCompanyProfile; window.quickWA=quickWA;

function showAuthTab(tab){
  document.getElementById('signin-form').style.display=tab==='signin'?'block':'none';
  document.getElementById('signup-form').style.display=tab==='signup'?'block':'none';
  document.querySelectorAll('.auth-tab').forEach((t,i)=>t.classList.toggle('active',(i===0&&tab==='signin')||(i===1&&tab==='signup')));
}
async function doSignIn(){
  const email=document.getElementById('signin-email').value.trim();
  const pass=document.getElementById('signin-password').value;
  const errEl=document.getElementById('signin-error');
  errEl.style.display='none';
  if(!email||!pass){errEl.textContent='Email aur password bharo!';errEl.style.display='block';return;}
  try{await signInWithEmailAndPassword(auth,email,pass);}
  catch(e){errEl.textContent=(e.code==='auth/invalid-credential'||e.code==='auth/wrong-password'||e.code==='auth/user-not-found')?'Email ya password galat hai!':'Error: '+e.message;errEl.style.display='block';}
}
async function doSignUp(){
  const name=document.getElementById('signup-name').value.trim();
  const email=document.getElementById('signup-email').value.trim();
  const pass=document.getElementById('signup-password').value;
  const errEl=document.getElementById('signup-error');
  errEl.style.display='none';
  if(!name||!email||!pass){errEl.textContent='Sab fields bharo!';errEl.style.display='block';return;}
  if(pass.length<6){errEl.textContent='Password 6+ characters!';errEl.style.display='block';return;}
  try{const c=await createUserWithEmailAndPassword(auth,email,pass);await updateProfile(c.user,{displayName:name});}
  catch(e){errEl.textContent=e.code==='auth/email-already-in-use'?'Email already registered!':'Error: '+e.message;errEl.style.display='block';}
}
async function doSignOut(){await signOut(auth);}

onAuthStateChanged(auth,user=>{
  if(user){
    currentUser=user;
    document.getElementById('auth-screen').style.display='none';
    document.getElementById('main-app').style.display='block';
    document.getElementById('greeting').textContent=`Namaste, ${user.displayName||user.email.split('@')[0]}! 👋`;
    initApp();
  }else{
    currentUser=null;
    document.getElementById('auth-screen').style.display='flex';
    document.getElementById('main-app').style.display='none';
  }
});

async function initApp(){
  const today=new Date().toISOString().split('T')[0];
  const thisMonth=today.substring(0,7);
  document.getElementById('bill-date').value=today;
  document.getElementById('exp-date').value=today;
  document.getElementById('billing-filter-month').value=thisMonth;
  document.getElementById('exp-filter-month').value=thisMonth;
  document.getElementById('bill-fy').value=getFY();
  await loadCompanyProfile();
  await loadClientsCache();
  loadHomeData();
  addTripRow();
}

function getFY(){const now=new Date();const y=now.getFullYear();const m=now.getMonth()+1;const s=m>=4?y:y-1;return`${String(s).slice(-2)}-${String(s+1).slice(-2)}`;}

async function loadCompanyProfile(){
  try{const snap=await getDoc(doc(db,'companyProfiles',currentUser.uid));if(snap.exists()){companyProfile=snap.data();populateCompanyForm();renderCompanyPreview();}}catch(e){console.error(e);}
}
function populateCompanyForm(){
  ['name','address','gstin','phone'].forEach(k=>{const el=document.getElementById('co-'+k);if(el)el.value=companyProfile[k]||'';});
  document.getElementById('co-bank-name').value=companyProfile.bankName||'';
  document.getElementById('co-acc').value=companyProfile.acc||'';
  document.getElementById('co-ifsc').value=companyProfile.ifsc||'';
}
function renderCompanyPreview(){
  document.getElementById('preview-company-name').textContent=companyProfile.name||'Company Set Nahi';
  document.getElementById('preview-company-gstin').textContent=companyProfile.gstin?'GSTIN: '+companyProfile.gstin:'';
  document.getElementById('preview-company-address').textContent=companyProfile.address||'';
  document.getElementById('preview-company-phone').textContent=companyProfile.phone?'📞 '+companyProfile.phone:'';
  const bank=[companyProfile.bankName,companyProfile.acc?'A/C: '+companyProfile.acc:'',companyProfile.ifsc?'IFSC: '+companyProfile.ifsc:''].filter(Boolean).join(' | ');
  document.getElementById('preview-company-bank').textContent=bank?'🏦 '+bank:'';
}
async function saveCompanyProfile(){
  const name=document.getElementById('co-name').value.trim();
  if(!name){showToast('Naam zaroori hai!','error');return;}
  const data={name,address:document.getElementById('co-address').value.trim(),gstin:document.getElementById('co-gstin').value.trim().toUpperCase(),phone:document.getElementById('co-phone').value.trim(),bankName:document.getElementById('co-bank-name').value.trim(),acc:document.getElementById('co-acc').value.trim(),ifsc:document.getElementById('co-ifsc').value.trim(),updatedAt:Timestamp.now()};
  try{await setDoc(doc(db,'companyProfiles',currentUser.uid),data);companyProfile=data;renderCompanyPreview();showToast('✅ Saved!','success');}catch(e){showToast('Error: '+e.message,'error');}
}

async function loadClientsCache(){
  try{const snap=await getDocs(query(collection(db,'clients'),where('uid','==',currentUser.uid),orderBy('name')));clientsCache=snap.docs.map(d=>({id:d.id,...d.data()}));updateClientDropdowns();renderClientsList();}catch(e){console.error(e);}
}
function updateClientDropdowns(){
  ['bill-client','billing-filter-client'].forEach(id=>{
    const sel=document.getElementById(id);const val=sel.value;
    sel.innerHTML=id==='bill-client'?'<option value="">-- Client chuniye --</option>':'<option value="">Sab</option>';
    clientsCache.forEach(c=>{const o=document.createElement('option');o.value=c.id;o.textContent=c.name;sel.appendChild(o);});
    sel.value=val;
  });
  document.getElementById('home-clients').textContent=clientsCache.length;
}
function renderClientsList(){
  const el=document.getElementById('clients-list');
  if(!clientsCache.length){el.innerHTML=`<div class="empty-state"><div class="empty-icon">👤</div><h3>Koi client nahi</h3></div>`;return;}
  el.innerHTML=clientsCache.map(c=>`<div class="client-item"><div class="client-avatar">${c.name.charAt(0)}</div><div class="client-info"><div class="client-name">${c.name}</div>${c.gstin?`<div class="client-detail" style="color:var(--blue);">GSTIN: ${c.gstin}</div>`:''} ${c.billingAddress?`<div class="client-detail">📍 ${c.billingAddress}</div>`:''} ${c.rate?`<div class="client-rate">₹${Number(c.rate).toLocaleString('en-IN')}/trip</div>`:''}</div><div style="display:flex;gap:6px;"><button class="icon-btn icon-btn-blue" onclick="window.editClient('${c.id}')">✏️</button><button class="icon-btn icon-btn-red" onclick="window.deleteClient('${c.id}')">🗑️</button></div></div>`).join('');
}
async function saveClient(){
  const name=document.getElementById('client-name').value.trim();
  if(!name){showToast('Naam zaroori!','error');return;}
  try{await addDoc(collection(db,'clients'),{uid:currentUser.uid,name,billingAddress:document.getElementById('client-billing-address').value.trim(),gstin:document.getElementById('client-gstin').value.trim().toUpperCase(),phone:document.getElementById('client-phone').value.trim(),rate:parseFloat(document.getElementById('client-rate').value)||0,createdAt:Timestamp.now()});closeModal('add-client-modal');['client-name','client-billing-address','client-gstin','client-phone','client-rate'].forEach(id=>document.getElementById(id).value='');await loadClientsCache();showToast('✅ Client added!','success');}catch(e){showToast('Error: '+e.message,'error');}
}
function editClient(id){
  const c=clientsCache.find(x=>x.id===id);if(!c)return;
  document.getElementById('edit-client-id').value=id;
  document.getElementById('edit-client-name').value=c.name;
  document.getElementById('edit-client-billing-address').value=c.billingAddress||'';
  document.getElementById('edit-client-gstin').value=c.gstin||'';
  document.getElementById('edit-client-phone').value=c.phone||'';
  document.getElementById('edit-client-rate').value=c.rate||'';
  openModal('edit-client-modal');
}
async function updateClient(){
  const id=document.getElementById('edit-client-id').value;
  const name=document.getElementById('edit-client-name').value.trim();
  if(!name){showToast('Naam zaroori!','error');return;}
  try{await updateDoc(doc(db,'clients',id),{name,billingAddress:document.getElementById('edit-client-billing-address').value.trim(),gstin:document.getElementById('edit-client-gstin').value.trim().toUpperCase(),phone:document.getElementById('edit-client-phone').value.trim(),rate:parseFloat(document.getElementById('edit-client-rate').value)||0});closeModal('edit-client-modal');await loadClientsCache();showToast('✅ Updated!','success');}catch(e){showToast('Error!','error');}
}
async function deleteClient(id){if(!confirm('Delete?'))return;try{await deleteDoc(doc(db,'clients',id));await loadClientsCache();showToast('🗑️ Deleted!','success');}catch(e){showToast('Error!','error');}}

function onBillClientChange(){
  const id=document.getElementById('bill-client').value;
  const preview=document.getElementById('bill-client-preview');
  if(!id){preview.style.display='none';return;}
  const c=clientsCache.find(x=>x.id===id);
  if(c){document.getElementById('bcp-name').textContent=c.name;document.getElementById('bcp-gstin').textContent=c.gstin?'GSTIN: '+c.gstin:'';document.getElementById('bcp-addr').textContent=c.billingAddress||'';preview.style.display='block';if(c.rate>0){document.querySelectorAll('.trip-rate-input').forEach(inp=>{if(!inp.value)inp.value=c.rate;});recalcTotal();}}
}

function addTripRow(){
  const id='trip-'+Date.now();
  const clientId=document.getElementById('bill-client').value;
  const client=clientsCache.find(c=>c.id===clientId);
  const defRate=client?.rate||'';
  const today=document.getElementById('bill-date').value||new Date().toISOString().split('T')[0];
  const row=document.createElement('div');
  row.className='trip-row';row.id=id;
  row.innerHTML=`<input type="date" value="${today}" onchange="window.recalcTotal()"><input type="text" placeholder="3090" style="text-transform:uppercase;" oninput="this.value=this.value.toUpperCase()"><input type="text" placeholder="BAZAR"><input type="number" class="trip-rate-input" placeholder="1200" value="${defRate}" oninput="window.recalcTotal()"><button class="del-trip-btn" onclick="window.removeTripRow('${id}')">✕</button>`;
  document.getElementById('trips-container').appendChild(row);
  recalcTotal();
}
function removeTripRow(id){const el=document.getElementById(id);if(el){el.remove();recalcTotal();}}
function recalcTotal(){
  const rows=document.querySelectorAll('#trips-container .trip-row');
  let subtotal=0;
  rows.forEach(r=>{subtotal+=parseFloat(r.querySelectorAll('input')[3].value)||0;});
  const gstType=document.getElementById('bill-gst-type').value;
  const gstRate=gstType==='none'?0:parseFloat(gstType);
  const gstAmt=subtotal*gstRate/100;
  const total=subtotal+gstAmt;
  document.getElementById('trip-subtotal').textContent='₹'+subtotal.toLocaleString('en-IN');
  document.getElementById('trip-total').textContent='₹'+total.toLocaleString('en-IN');
  const gd=document.getElementById('gst-breakdown');
  if(gstRate>0){gd.style.display='block';document.getElementById('cgst-label').textContent=`CGST @ ${gstRate/2}%`;document.getElementById('sgst-label').textContent=`SGST @ ${gstRate/2}%`;document.getElementById('cgst-val').textContent='₹'+(gstAmt/2).toFixed(2);document.getElementById('sgst-val').textContent='₹'+(gstAmt/2).toFixed(2);}
  else{gd.style.display='none';}
}

async function saveBill(showInvoice){
  const clientId=document.getElementById('bill-client').value;
  const date=document.getElementById('bill-date').value;
  const billNo=document.getElementById('bill-no').value.trim();
  const fy=document.getElementById('bill-fy').value.trim();
  const gstType=document.getElementById('bill-gst-type').value;
  if(!clientId){showToast('Client chuniye!','error');return;}
  if(!date||!billNo){showToast('Date aur Bill No. bharo!','error');return;}
  const client=clientsCache.find(c=>c.id===clientId);if(!client)return;
  const rows=document.querySelectorAll('#trips-container .trip-row');
  const trips=[];
  rows.forEach(r=>{const inputs=r.querySelectorAll('input');const rate=parseFloat(inputs[3].value)||0;if(rate>0||inputs[1].value||inputs[2].value)trips.push({date:inputs[0].value,vehicle:inputs[1].value.trim().toUpperCase(),destination:inputs[2].value.trim().toUpperCase(),rate});});
  if(!trips.length){showToast('Ek trip add karo!','error');return;}
  const subtotal=trips.reduce((s,t)=>s+t.rate,0);
  const gstRate=gstType==='none'?0:parseFloat(gstType);
  const gstAmt=subtotal*gstRate/100;
  const billData={uid:currentUser.uid,clientId,clientName:client.name,clientGSTIN:client.gstin||'',clientBillingAddress:client.billingAddress||'',clientPhone:client.phone||'',sellerName:companyProfile.name||'',sellerAddress:companyProfile.address||'',sellerGSTIN:companyProfile.gstin||'',sellerPhone:companyProfile.phone||'',sellerBankName:companyProfile.bankName||'',sellerAcc:companyProfile.acc||'',sellerIFSC:companyProfile.ifsc||'',trips,subtotal,gstRate,gstAmt,cgst:gstAmt/2,sgst:gstAmt/2,total:subtotal+gstAmt,billNo,billDate:date,fy,isGST:gstRate>0,createdAt:Timestamp.now()};
  try{
    const ref=await addDoc(collection(db,'bills'),billData);
    showToast('✅ Bill saved!','success');
    if(showInvoice){currentInvoiceBill={...billData,id:ref.id};showInvoiceModal(currentInvoiceBill);}
    document.getElementById('bill-client').value='';document.getElementById('bill-client-preview').style.display='none';document.getElementById('bill-no').value='';document.getElementById('bill-gst-type').value='none';document.getElementById('trips-container').innerHTML='';addTripRow();recalcTotal();loadHomeData();
  }catch(e){showToast('Error: '+e.message,'error');}
}

function showInvoiceModal(bill){currentInvoiceBill=bill;document.getElementById('invoice-preview-box').innerHTML=generateInvoiceHTML(bill);openModal('invoice-modal');}
function viewInvoice(bs){const bill=JSON.parse(decodeURIComponent(bs));currentInvoiceBill=bill;document.getElementById('invoice-preview-box').innerHTML=generateInvoiceHTML(bill);openModal('invoice-modal');}

function generateInvoiceHTML(bill){
  const s=bill;
  const tripRows=bill.trips.map(t=>`<tr><td style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;">${t.date?formatDate(t.date):''}</td><td style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;font-weight:600;">${t.vehicle||''}</td><td style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;">${t.destination||''}</td><td style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;text-align:right;">${t.rate?t.rate.toLocaleString('en-IN'):''}</td></tr>`).join('');
  const gstRows=bill.gstRate>0?`<tr><td colspan="3" style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;">CGST @ ${bill.gstRate/2}%</td><td style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;text-align:right;">${bill.cgst.toFixed(2)}</td></tr><tr><td colspan="3" style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;">SGST @ ${bill.gstRate/2}%</td><td style="padding:5px 8px;border:1px solid #ccc;font-size:0.8rem;text-align:right;">${bill.sgst.toFixed(2)}</td></tr>`:'';
  return `<div id="gst-printable" style="background:white;color:#111;font-family:Arial,sans-serif;padding:16px;border-radius:8px;font-size:0.82rem;"><div style="text-align:center;border-bottom:2px solid #111;padding-bottom:10px;margin-bottom:10px;"><div style="font-size:1.2rem;font-weight:800;">${s.sellerName||'COMPANY'}</div>${s.sellerAddress?`<div style="font-size:0.78rem;">${s.sellerAddress}</div>`:''} ${s.sellerGSTIN?`<div style="font-size:0.78rem;font-weight:700;">GSTIN :- ${s.sellerGSTIN}</div>`:''} ${s.sellerPhone?`<div style="font-size:0.78rem;">MOBILE :- ${s.sellerPhone}</div>`:''}</div><table style="width:100%;border-collapse:collapse;margin-bottom:8px;"><tr><td style="border:1px solid #111;padding:5px 8px;width:60%;vertical-align:top;"><div style="font-size:0.72rem;font-weight:700;">BILL TO</div><div style="font-weight:700;font-size:0.9rem;">${s.clientName}</div>${s.clientBillingAddress?`<div style="font-size:0.75rem;">${s.clientBillingAddress}</div>`:''} ${s.clientGSTIN?`<div style="font-size:0.75rem;font-weight:700;">GSTIN :- ${s.clientGSTIN}</div>`:''}</td><td style="border:1px solid #111;padding:5px 8px;vertical-align:top;"><div><span style="font-weight:700;">Date:-</span> ${formatDate(s.billDate)}</div><div><span style="font-weight:700;">BILL NO-</span> ${s.billNo}${s.fy?' / '+s.fy:''}</div></td></tr></table><table style="width:100%;border-collapse:collapse;margin-bottom:8px;"><thead><tr style="background:#f0f0f0;"><th style="padding:6px 8px;border:1px solid #ccc;text-align:left;font-size:0.78rem;">DATE</th><th style="padding:6px 8px;border:1px solid #ccc;text-align:left;font-size:0.78rem;">VEHICLE NO.</th><th style="padding:6px 8px;border:1px solid #ccc;text-align:left;font-size:0.78rem;">DESTINATION</th><th style="padding:6px 8px;border:1px solid #ccc;text-align:right;font-size:0.78rem;">RATE</th></tr></thead><tbody>${tripRows}${gstRows}<tr style="font-weight:800;background:#f9f9f9;"><td colspan="3" style="padding:7px 8px;border:1px solid #ccc;text-align:right;">TOTAL</td><td style="padding:7px 8px;border:1px solid #ccc;text-align:right;font-size:0.9rem;">${bill.total.toLocaleString('en-IN')}</td></tr></tbody></table><div style="border:1px solid #ccc;padding:6px 8px;margin-bottom:8px;font-size:0.78rem;font-weight:700;">${amountInWords(bill.total)}</div>${(s.sellerBankName||s.sellerAcc||s.sellerIFSC)?`<div style="border:1px solid #ccc;padding:8px;text-align:center;font-size:0.78rem;"><div style="font-weight:700;margin-bottom:4px;">BANK DETAIL:-</div>${s.sellerBankName?`<div style="font-weight:700;">${s.sellerBankName}</div>`:''} ${s.sellerAcc?`<div>ACCOUNT NO:- ${s.sellerAcc}</div>`:''} ${s.sellerIFSC?`<div>IFSC :- ${s.sellerIFSC}</div>`:''}</div>`:''}</div>`;
}

function amountInWords(amount){
  const ones=['','ONE','TWO','THREE','FOUR','FIVE','SIX','SEVEN','EIGHT','NINE','TEN','ELEVEN','TWELVE','THIRTEEN','FOURTEEN','FIFTEEN','SIXTEEN','SEVENTEEN','EIGHTEEN','NINETEEN'];
  const tens=['','','TWENTY','THIRTY','FORTY','FIFTY','SIXTY','SEVENTY','EIGHTY','NINETY'];
  function convert(n){if(n===0)return'';if(n<20)return ones[n]+' ';if(n<100)return tens[Math.floor(n/10)]+' '+(n%10?ones[n%10]+' ':'');if(n<1000)return ones[Math.floor(n/100)]+' HUNDRED '+(n%100?convert(n%100):'');if(n<100000)return convert(Math.floor(n/1000))+'THOUSAND '+(n%1000?convert(n%1000):'');if(n<10000000)return convert(Math.floor(n/100000))+'LAKH '+(n%100000?convert(n%100000):'');return convert(Math.floor(n/10000000))+'CRORE '+(n%10000000?convert(n%10000000):'');}
  const int=Math.floor(amount);const dec=Math.round((amount-int)*100);
  return convert(int).trim()+' RUPEES'+(dec>0?' AND '+convert(dec).trim()+' PAISE':'')+' ONLY.';
}

function downloadInvoicePDF(){
  if(!currentInvoiceBill){showToast('No invoice!','error');return;}
  const {jsPDF}=window.jspdf;const pdfdoc=new jsPDF({unit:'mm',format:'a4'});const b=currentInvoiceBill;let y=15;const lm=15,rm=195,cw=rm-lm;
  pdfdoc.setFontSize(14);pdfdoc.setFont('helvetica','bold');pdfdoc.text(b.sellerName||'COMPANY',105,y,{align:'center'});y+=7;
  pdfdoc.setFontSize(9);pdfdoc.setFont('helvetica','normal');
  if(b.sellerAddress){pdfdoc.text(b.sellerAddress,105,y,{align:'center'});y+=5;}
  if(b.sellerGSTIN){pdfdoc.text('GSTIN :- '+b.sellerGSTIN,105,y,{align:'center'});y+=5;}
  if(b.sellerPhone){pdfdoc.text('MOBILE :- '+b.sellerPhone,105,y,{align:'center'});y+=5;}
  pdfdoc.setLineWidth(0.5);pdfdoc.line(lm,y,rm,y);y+=5;
  pdfdoc.rect(lm,y,cw*0.6,22);pdfdoc.rect(lm+cw*0.6,y,cw*0.4,22);
  pdfdoc.setFontSize(7);pdfdoc.setFont('helvetica','bold');pdfdoc.text('BILL TO',lm+2,y+5);
  pdfdoc.setFontSize(9);pdfdoc.text(b.clientName,lm+2,y+10);
  pdfdoc.setFont('helvetica','normal');pdfdoc.setFontSize(7.5);
  if(b.clientBillingAddress){const lines=pdfdoc.splitTextToSize(b.clientBillingAddress,cw*0.58-4);pdfdoc.text(lines,lm+2,y+15);}
  if(b.clientGSTIN){pdfdoc.setFont('helvetica','bold');pdfdoc.text('GSTIN :- '+b.clientGSTIN,lm+2,y+19);}
  pdfdoc.setFont('helvetica','bold');pdfdoc.setFontSize(8.5);
  pdfdoc.text('Date:- '+formatDate(b.billDate),lm+cw*0.6+2,y+7);
  pdfdoc.text('BILL NO- '+b.billNo+(b.fy?' / '+b.fy:''),lm+cw*0.6+2,y+13);y+=27;
  const tableHead=[['DATE','VEHICLE NO.','DESTINATION','RATE']];
  const tableBody=b.trips.map(t=>[t.date?formatDate(t.date):'',t.vehicle||'',t.destination||'',t.rate?t.rate.toLocaleString('en-IN'):'']);
  if(b.gstRate>0){tableBody.push(['','','CGST @ '+b.gstRate/2+'%',b.cgst.toFixed(2)]);tableBody.push(['','','SGST @ '+b.gstRate/2+'%',b.sgst.toFixed(2)]);}
  tableBody.push(['','','TOTAL',b.total.toLocaleString('en-IN')]);
  pdfdoc.autoTable({startY:y,head:tableHead,body:tableBody,margin:{left:lm,right:15},headStyles:{fillColor:[220,220,220],textColor:[0,0,0],fontStyle:'bold',fontSize:8},bodyStyles:{fontSize:8.5,textColor:[0,0,0]},columnStyles:{0:{cellWidth:30},1:{cellWidth:25},2:{cellWidth:80},3:{cellWidth:35,halign:'right',fontStyle:'bold'}},didParseCell:(data)=>{if(data.row.index===tableBody.length-1&&data.section==='body'){data.cell.styles.fontStyle='bold';data.cell.styles.fillColor=[245,245,245];}}});
  y=pdfdoc.lastAutoTable.finalY+4;
  const words=amountInWords(b.total);const wordLines=pdfdoc.splitTextToSize(words,cw-4);
  pdfdoc.rect(lm,y,cw,wordLines.length*5+6);pdfdoc.setFont('helvetica','bold');pdfdoc.setFontSize(8);pdfdoc.text(wordLines,lm+2,y+5);y+=wordLines.length*5+10;
  if(b.sellerBankName||b.sellerAcc||b.sellerIFSC){pdfdoc.setFont('helvetica','bold');pdfdoc.text('BANK DETAIL:-',105,y,{align:'center'});y+=5;pdfdoc.setFont('helvetica','normal');if(b.sellerBankName){pdfdoc.text(b.sellerBankName,105,y,{align:'center'});y+=5;}if(b.sellerAcc){pdfdoc.text('ACCOUNT NO:- '+b.sellerAcc,105,y,{align:'center'});y+=5;}if(b.sellerIFSC){pdfdoc.text('IFSC :- '+b.sellerIFSC,105,y,{align:'center'});}}
  pdfdoc.save(`Bill-${b.billNo}-${b.clientName}.pdf`);showToast('📥 PDF downloaded!','success');
}

function printInvoice(){
  const el=document.getElementById('gst-printable');if(!el)return;
  const w=window.open('','_blank');
  w.document.write(`<html><head><title>Bill</title><style>body{font-family:Arial,sans-serif;padding:20px;color:#111;max-width:700px;margin:0 auto;}table{width:100%;border-collapse:collapse;}th,td{padding:6px 8px;border:1px solid #ccc;font-size:0.85rem;}th{background:#f0f0f0;}@media print{body{padding:10px;}}</style></head><body>`);

w.document.write(el.outerHTML);w.document.write(’</body></html>’);w.document.close();setTimeout(()=>w.print(),600);
}

function shareInvoiceWA(){
if(!currentInvoiceBill){showToast(‘Pehle invoice!’,‘error’);return;}
const b=currentInvoiceBill;
const tripLines=b.trips.map(t=>`${t.date?formatDate(t.date):''} | ${t.vehicle||''} | ${t.destination||''} | ₹${t.rate||0}`).join(’\n’);
const text=[`🧾 *BILL*`,`━━━━━━━━━━━━━━━━━`,`🏢 *${b.sellerName||''}*`,b.sellerGSTIN?`GSTIN: ${b.sellerGSTIN}`:’’,`📋 Bill No: ${b.billNo}${b.fy?' / '+b.fy:''}  |  Date: ${formatDate(b.billDate)}`,`👤 ${b.clientName}`,b.clientGSTIN?`GSTIN: ${b.clientGSTIN}`:’’,b.clientBillingAddress?`📍 ${b.clientBillingAddress}`:’’,`,`*DATE | VEHICLE | DESTINATION | RATE*`,tripLines,`━━━━━━━━━━━━━━━━━`,b.gstRate>0?`CGST: ₹${b.cgst.toFixed(2)}  SGST: ₹${b.sgst.toFixed(2)}`:'',`💰 *TOTAL: ₹${b.total.toLocaleString('en-IN')}*`,`━━━━━━━━━━━━━━━━━`,b.sellerBankName?`🏦 ${b.sellerBankName}`:'',b.sellerAcc?`A/C: ${b.sellerAcc}`:'',b.sellerIFSC?`IFSC: ${b.sellerIFSC}`:'',`_FleetBook_`].filter(l=>l!=='').join('\n'); window.open(`https://wa.me/?text=${encodeURIComponent(text)}`,'_blank'); } function copyInvoiceText(){ if(!currentInvoiceBill){showToast('Pehle invoice!','error');return;} const b=currentInvoiceBill; const t=[`Bill No: ${b.billNo}  Date: ${formatDate(b.billDate)}`,`From: ${b.sellerName}`,`To: ${b.clientName}`,`,b.trips.map(t=>`${t.date?formatDate(t.date):''} | ${t.vehicle} | ${t.destination} | Rs.${t.rate}`).join(’\n’),``,`TOTAL: Rs.${b.total.toLocaleString('en-IN')}`].join(’\n’);
navigator.clipboard.writeText(t).then(()=>showToast(‘📋 Copied!’,‘success’)).catch(()=>showToast(‘Copy failed!’,‘error’));
}
function quickWA(bs){const bill=JSON.parse(decodeURIComponent(bs));currentInvoiceBill=bill;shareInvoiceWA();}

async function loadBills(){
const el=document.getElementById(‘bills-list’),summaryEl=document.getElementById(‘billing-summary’);
el.innerHTML=’<div class="loading"><div class="spinner"></div>Loading…</div>’;
try{
const snap=await getDocs(query(collection(db,‘bills’),where(‘uid’,’==’,currentUser.uid),orderBy(‘billDate’,‘desc’),orderBy(‘createdAt’,‘desc’)));
let bills=snap.docs.map(d=>({id:d.id,…d.data()}));
const thisMonth=new Date().toISOString().substring(0,7);
const fc=document.getElementById(‘billing-filter-client’).value;
const fm=document.getElementById(‘billing-filter-month’).value;
if(currentBillingTab===‘month’)bills=bills.filter(b=>b.billDate&&b.billDate.startsWith(thisMonth));
else if(currentBillingTab===‘gst’)bills=bills.filter(b=>b.isGST);
else if(fm)bills=bills.filter(b=>b.billDate&&b.billDate.startsWith(fm));
if(fc)bills=bills.filter(b=>b.clientId===fc);
const tAmt=bills.reduce((s,b)=>s+(b.total||0),0);
summaryEl.innerHTML=`<div class="summary-pill"><div class="label">Bills</div><div class="value blue">${bills.length}</div></div><div class="summary-pill"><div class="label">Total</div><div class="value green">₹${tAmt.toLocaleString('en-IN',{maximumFractionDigits:0})}</div></div>`;
if(!bills.length){el.innerHTML=`<div class="empty-state"><div class="empty-icon">📋</div><h3>Koi bill nahi</h3></div>`;return;}
el.innerHTML=bills.map(b=>{const bs=encodeURIComponent(JSON.stringify(b));const tc=b.trips?b.trips.length:0;return`<div class="bill-card"><div class="bill-header"><div><div class="bill-client">${b.clientName} ${b.isGST?'<span class="gst-badge">GST</span>':''}</div><div class="bill-date">📅 ${formatDate(b.billDate)} • Bill #${b.billNo}${b.fy?' / '+b.fy:''}</div><div class="bill-detail">🚛 ${tc} trip${tc!==1?'s':''}</div></div><div style="text-align:right;"><div class="bill-amount">₹${(b.total||0).toLocaleString('en-IN',{maximumFractionDigits:0})}</div></div></div><div class="bill-actions"><button class="btn btn-blue" style="padding:6px 12px;font-size:0.78rem;" onclick="window.viewInvoice('${bs}')">🧾 Invoice</button><button class="btn btn-whatsapp" style="padding:6px 12px;font-size:0.78rem;" onclick="window.quickWA('${bs}')">💬 WA</button><button class="btn btn-red" style="padding:6px 12px;font-size:0.78rem;" onclick="window.deleteBill('${b.id}')">🗑️</button></div></div>`;}).join(’’);
}catch(e){el.innerHTML=`<div style="color:var(--red);padding:20px;">Error: ${e.message}</div>`;}
}
async function deleteBill(id){if(!confirm(‘Delete?’))return;try{await deleteDoc(doc(db,‘bills’,id));showToast(‘🗑️ Deleted!’,‘success’);loadBills();loadHomeData();}catch(e){showToast(‘Error!’,‘error’);}}
function switchBillingTab(tab){currentBillingTab=tab;document.querySelectorAll(’#page-billing .tab-btn’).forEach((b,i)=>b.classList.toggle(‘active’,[‘all’,‘month’,‘gst’][i]===tab));loadBills();}

async function saveExpense(){
const date=document.getElementById(‘exp-date’).value;const amount=parseFloat(document.getElementById(‘exp-amount’).value);
if(!date||!amount){showToast(‘Date aur amount zaroori!’,‘error’);return;}
try{await addDoc(collection(db,‘expenses’),{uid:currentUser.uid,date,amount,category:document.getElementById(‘exp-category’).value,vehicle:document.getElementById(‘exp-vehicle’).value.trim(),payment:document.getElementById(‘exp-payment’).value,note:document.getElementById(‘exp-note’).value.trim(),createdAt:Timestamp.now()});document.getElementById(‘exp-amount’).value=’’;document.getElementById(‘exp-note’).value=’’;showToast(‘✅ Saved!’,‘success’);loadHomeData();}catch(e){showToast(‘Error!’,‘error’);}
}
async function loadExpenses(){
const el=document.getElementById(‘expenses-list’);el.innerHTML=’<div class="loading"><div class="spinner"></div>Loading…</div>’;
const fm=document.getElementById(‘exp-filter-month’).value;
try{const snap=await getDocs(query(collection(db,‘expenses’),where(‘uid’,’==’,currentUser.uid),orderBy(‘date’,‘desc’),orderBy(‘createdAt’,‘desc’)));let exps=snap.docs.map(d=>({id:d.id,…d.data()}));if(fm)exps=exps.filter(e=>e.date.startsWith(fm));const total=exps.reduce((s,e)=>s+e.amount,0);if(!exps.length){el.innerHTML=`<div class="empty-state"><div class="empty-icon">🚛</div><h3>Koi expense nahi</h3></div>`;return;}el.innerHTML=`<div style="background:rgba(255,71,87,0.1);border:1px solid rgba(255,71,87,0.3);border-radius:12px;padding:14px;margin-bottom:12px;display:flex;justify-content:space-between;"><span style="color:var(--text-muted);font-weight:600;">Total</span><span style="font-family:'Baloo 2',sans-serif;font-size:1.2rem;font-weight:800;color:var(--red);">₹${total.toLocaleString('en-IN')}</span></div>`+exps.map(e=>`<div style="background:rgba(255,255,255,0.04);border-radius:12px;padding:14px;margin-bottom:8px;border:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;"><div><div style="font-weight:700;">${e.category}${e.vehicle?' • '+e.vehicle:''}</div><div style="font-size:0.78rem;color:var(--text-muted);">📅 ${formatDate(e.date)} • ${e.payment}${e.note?' • '+e.note:''}</div></div><div style="text-align:right;"><div style="font-family:'Baloo 2',sans-serif;font-size:1.1rem;font-weight:800;color:var(--red);">₹${e.amount.toLocaleString('en-IN')}</div><button onclick="window.deleteExpense('${e.id}')" style="background:rgba(255,71,87,0.15);color:var(--red);border:none;border-radius:6px;padding:3px 8px;font-size:0.75rem;cursor:pointer;margin-top:4px;">🗑️</button></div></div>`).join(’’);}catch(e){el.innerHTML=`<div style="color:var(--red);">Error!</div>`;}
}
async function deleteExpense(id){if(!confirm(‘Delete?’))return;try{await deleteDoc(doc(db,‘expenses’,id));showToast(‘🗑️ Deleted!’,‘success’);loadExpenses();loadHomeData();}catch(e){showToast(‘Error!’,‘error’);}}
function switchExpenseTab(tab){currentExpenseTab=tab;[‘add’,‘list’].forEach(t=>document.getElementById(`expense-tab-${t}`).style.display=t===tab?‘block’:‘none’);document.querySelectorAll(’#page-expense .tab-btn’).forEach((b,i)=>b.classList.toggle(‘active’,[‘add’,‘list’][i]===tab));if(tab===‘list’)loadExpenses();}

async function loadHomeData(){
const thisMonth=new Date().toISOString().substring(0,7);
try{
const billsSnap=await getDocs(query(collection(db,‘bills’),where(‘uid’,’==’,currentUser.uid)));
const bills=billsSnap.docs.map(d=>d.data());
document.getElementById(‘home-month-billing’).textContent=‘₹’+bills.filter(b=>b.billDate&&b.billDate.startsWith(thisMonth)).reduce((s,b)=>s+(b.total||0),0).toLocaleString(‘en-IN’,{maximumFractionDigits:0});
document.getElementById(‘home-total-bills’).textContent=bills.length;
const expSnap=await getDocs(query(collection(db,‘expenses’),where(‘uid’,’==’,currentUser.uid)));
const exps=expSnap.docs.map(d=>d.data());
document.getElementById(‘home-month-expense’).textContent=‘₹’+exps.filter(e=>e.date&&e.date.startsWith(thisMonth)).reduce((s,e)=>s+e.amount,0).toLocaleString(‘en-IN’);
const sorted=bills.sort((a,b)=>(b.billDate||’’).localeCompare(a.billDate||’’)).slice(0,5);
const recentEl=document.getElementById(‘home-recent-bills’);
if(!sorted.length){recentEl.innerHTML=`<div class="empty-state" style="padding:20px;"><div class="empty-icon">📋</div><p>Koi bill nahi</p></div>`;return;}
recentEl.innerHTML=sorted.map(b=>`<div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid var(--border);"><div><div style="font-weight:700;font-size:0.9rem;">${b.clientName}</div><div style="font-size:0.75rem;color:var(--text-muted);">${formatDate(b.billDate)} • ${b.trips?b.trips.length:0} trips • #${b.billNo}</div></div><div style="font-family:'Baloo 2',sans-serif;font-weight:800;color:var(--green);">₹${(b.total||0).toLocaleString('en-IN',{maximumFractionDigits:0})}</div></div>`).join(’’);
}catch(e){console.error(e);}
}

function showPage(page){
document.querySelectorAll(’.page’).forEach(p=>p.classList.remove(‘active’));
document.querySelectorAll(’.nav-item’).forEach(n=>n.classList.remove(‘active’));
document.getElementById(`page-${page}`).classList.add(‘active’);
const idx=[‘home’,‘create’,‘billing’,‘clients’,‘expense’,‘settings’].indexOf(page);
if(idx>=0)document.querySelectorAll(’.nav-item’)[idx].classList.add(‘active’);
if(page===‘billing’)loadBills();
if(page===‘expense’&&currentExpenseTab===‘list’)loadExpenses();
}
function openModal(id){document.getElementById(id).classList.add(‘open’);}
function closeModal(id){document.getElementById(id).classList.remove(‘open’);}
document.querySelectorAll(’.modal-overlay’).forEach(m=>m.addEventListener(‘click’,e=>{if(e.target===m)m.classList.remove(‘open’);}));
function showToast(msg,type=‘success’){const t=document.getElementById(‘toast’);t.textContent=msg;t.className=`toast ${type} show`;setTimeout(()=>t.classList.remove(‘show’),3000);}
function formatDate(d){if(!d)return’’;return new Date(d+‘T00:00:00’).toLocaleDateString(‘en-IN’,{day:‘2-digit’,month:‘2-digit’,year:‘numeric’});}
</script>

</body>
</html>